{
  "schemaVersion": 1,
  "id": "jandragsbaek.wpm",
  "name": "WPM",
  "version": "0.1.0",
  "author": "jandragsbaek",
  "license": "MIT",
  "description": "Live WPM from actual keystrokes, per window, with git-style diffs and a daily Obsidian writing goal.",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "WPM",
    "description": "Typing speed, per-app git diffs, and a daily word goal",
    "category": "Productivity",
    "defaultSection": "right",
    "allowMultiple": false,
    "defaults": {
      "goalWords": 1000,
      "goalMatch": "obsidian",
      "goalAppClass": "obsidian",
      "goals": [],
      "dailyNotePath": "",
      "autoExport": false
    },
    "schema": [
      {
        "key": "goalWords",
        "type": "integer",
        "label": "Daily word goal",
        "description": "Net words to hit each day for the primary match.",
        "min": 0,
        "max": 100000,
        "step": 50,
        "defaultValue": 1000
      },
      {
        "key": "goalMatch",
        "type": "string",
        "label": "Goal match",
        "description": "What to count: a Hyprland class (obsidian), browser site (site:x), herdr workspace (herdr:grok build), activity (activity:grok · grok build), Hyprland workspace (ws:2), or all.",
        "defaultValue": "obsidian"
      },
      {
        "key": "goalAppClass",
        "type": "string",
        "label": "Goal app class (legacy)",
        "description": "Used if Goal match is empty. Prefer Goal match.",
        "defaultValue": "obsidian"
      },
      {
        "key": "goals",
        "type": "string",
        "label": "Extra goals (JSON)",
        "description": "Optional JSON array of {match, words, label} for multiple targets. When set, this list is the full set of goals.",
        "defaultValue": ""
      },
      {
        "key": "dailyNotePath",
        "type": "string",
        "label": "Daily note path",
        "description": "Obsidian daily-note path. Tokens: {date} {yyyy} {MM} {dd}. Empty disables auto-write.",
        "defaultValue": ""
      },
      {
        "key": "autoExport",
        "type": "boolean",
        "label": "Update daily note",
        "description": "Rewrite the marked section in today's daily note as you type.",
        "defaultValue": false
      }
    ]
  }
}
